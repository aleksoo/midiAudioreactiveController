#ifndef STRUCTURES_H
#define STRUCTURES_H

struct valuesData{
			int *cc; // random number
			int *inputNumber;
};

struct menuData{ // TODO: clear up values and names
			int *cc; // random number
			int inputNumber = 0;
			int *position = 0;
			int *range = 0;
};

#endif